// regular function (function declaration)
function sayHi(name) {
  return `Hello ${name}`;
}

// arrow function
const sayHello = (name) => {
  return `Hello ${name}`;
};

// one line arrow function
const sayHey = (name, rate, color) => `Hello ${name}`;
